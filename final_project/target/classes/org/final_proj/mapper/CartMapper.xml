<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.final_proj.mapper.CartMapper">
	<!-- 사용자의 카트에 있는 제품을 전체 불러옴 -->
	<select id="getCart" resultType="org.final_proj.domain.CartDTO">
		SELECT * FROM cart
		WHERE userId LIKE #{userId}
	</select>
	
	<!-- 사용자의 카트에 제품을 등록함 -->
	<insert id="addToCart">
		INSERT INTO cart (userId, goodsId, amount, unitPrice, totalPrice)
		VALUES (#{userId}, #{goodsId}, #{amount}, ${unitPrice}, #{totalPrice});
	</insert>
</mapper>