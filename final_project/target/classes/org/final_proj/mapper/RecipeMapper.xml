<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.final_proj.mapper.RecipeMapper">
	<select id="getList" resultType="org.final_proj.domain.RecipeVO">
		<![CDATA[
			SELECT 	id, name, way, kind, hashtag, thumbnail_l,
					thumbnail, ingredient, kcal, manual_1, photo_1,
					manual_2, photo_2, manual_3, photo_3,
					manual_4, photo_4, manual_5, photo_5
			FROM recipe
			WHERE id > 0
			ORDER BY id DESC	
		]]>
	</select>


	<select id="getDetail" resultType="org.final_proj.domain.RecipeVO">
		SELECT * FROM recipe
		WHERE id = #{id}
	</select>
	
	<select id="getListWithPaging" resultType="org.final_proj.domain.RecipeVO">
		<![CDATA[
			SELECT id, name
			FROM 	(SELECT /*+INDEX_DESC(recipe SYS_C007584)*/
					ROWNUM subrn, id, name, way, kind, thumbnail_l, thumbnail, ingredient, kcal,
					manual_1, photo_1,manual_2, photo_2, manual_3, photo_3,
					manual_4, photo_4,manual_5, photo_5
					FROM recipe
					WHERE 
		]]>
	<![CDATA[ 
			ROWNUM <= #{pageNum} * #{amount})
			WHERE subrn > (#{pageNum} - 1) * #{amount}
		]]>
	
	</select>
	
	<select id="searchResult" resultType="org.final_proj.domain.RecipeVO">
		<![CDATA[
			SELECT id, name, thumbnail, way, kind, ingredient
			FROM recipe
			WHERE
		]]>
		<include refid="search"></include>
		id > 0
	</select>
	
	<select id="getTotalNumber" resultType="int">
		SELECT COUNT(*)
		FROM recipe
		WHERE
		<include refid="search"></include>
		id > 0
	</select>
	
	<sql id="search">
		<trim prefix="(" suffix=") AND " prefixOverrides="OR">
			<foreach collection="typeArr" item='type'>
				<trim prefix="OR">
					<choose>
						<when test="type == 'I'.toString()" >
							ingredient LIKE '%'||#{query}||'%'
						</when>
						<when test="type == 'N'.toString()">
							name LIKE '%'||#{query}||'%'
						</when>
						<when test="type == 'K'.toString()">
							kind LIKE '%'||#{query}||'%'
						</when>
					</choose>
				</trim>
			</foreach>
		</trim>
	</sql>
	
	<select id="searchWay" resultType="String">
		SELECT DISTINCT way 
		FROM recipe 
		WHERE
		<include refid="search"></include>
		id > 0
	</select>
	
	<select id="searchKind" resultType="String">
		SELECT DISTINCT kind 
		FROM recipe 
		WHERE
		<include refid="search"></include>
		id > 0
	</select>
	
</mapper>